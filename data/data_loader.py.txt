import yfinance as yf
import pandas as pd

def fetch_stock_data(ticker: str, start_date: str, end_date: str) -> pd.DataFrame:
    print(f"[{ticker}] verileri indiriliyor...")
    stock = yf.Ticker(ticker)
    df = stock.history(start=start_date, end=end_date)
    return df